definitions:
  Organization:
    properties:
      id:
        type: string
        format: uuid
      name:
        type: string
      avatarUrl:
        type: string
        format: uri
      description:
        type: string
      creatorId:
        type: string
        format: uuid
      gitlabId:
        type: integer
      jiraId:
        type: string
      createdAt:
        type: string
        format: date
      updatedAt:
        type: string
        format: date
      userLabels:
        type: array
        items:
          type: string
      creator:
        $ref: '#/definitions/User'
      projects:
        type: array
        items:
          $ref: '#/definitions/Project'
      members:
        type: array
        items:
          $ref: '#/definitions/User'
      labels:
        type: array
        items:
          $ref: '#/definitions/Label'
